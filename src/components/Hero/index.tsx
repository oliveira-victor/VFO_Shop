import * as S from './styles'

import heroImg from '../../assets/hero.webp'

const Hero = () => {
    return (
        
        <S.Hero style={{backgroundImage: `url(${heroImg})`}}>
        </S.Hero>
    )
}

export default Hero